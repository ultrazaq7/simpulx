<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- WebRTC audio calling -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Keep an active call alive + show its ongoing notification when minimized -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" tools:replace="android:maxSdkVersion" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Share-location composer: pin the agent's current position on the map. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Declaring a location or camera PERMISSION makes Android implicitly mark
         the matching HARDWARE as REQUIRED, which silently excludes devices that
         lack it (GPS-less rugged handhelds, camera-less tablets) from installing
         at all. None of it is required to run Simpulx: Send Location and photo
         capture are optional. Mark every implied feature non-required so the app
         stays installable everywhere and those features just no-op where absent. -->
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Simpulx"
        android:name=".SimpulxApplication"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- Google Maps SDK (Android) key — powers the Send Location map picker.
             Restrict this key to this app's package + SHA-1 and the Maps SDK for
             Android + Places API in the Google Cloud console. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCD19fHacXP4HkP9fDfRzibwRbnx4hPXNs" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep links: simpulx://conversation/{id}, contact/{id}, dashboard, followups -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="simpulx"/>
            </intent-filter>
        </activity>

        <!-- Custom FCM service: intercepts data messages and builds native
             WhatsApp-style notifications with avatar+badge BEFORE Flutter.
             tools:node="replace" overrides the firebase_messaging plugin's service. -->
        <service
            android:name=".SimpulxMessagingService"
            android:exported="false"
            tools:node="replace">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- Foreground service that keeps an active call alive + shows its
             WhatsApp-style ongoing-call notification while the app is minimized. -->
        <service
            android:name=".CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- FCM: default channel + icon used for background/data-only notifications.
             Keep the channel id in sync with NotificationHelper.CHANNEL_ID. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="incoming_message_v3" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Native notification reply/mark-read/reject-call handler -->
        <receiver
            android:name=".ReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="simpulx.ACTION_REPLY"/>
                <action android:name="simpulx.ACTION_MARK_AS_READ"/>
                <action android:name="simpulx.ACTION_REJECT_CALL"/>
                <action android:name="simpulx.ACTION_HANGUP_CALL"/>
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Phone dialer (tel:) so launchUrl can resolve it on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
