import DashboardView from "../DashboardView";

// Standalone Creative Insights report (own URL + tab/meta title via Shell).
export default function CreativeInsightsPage() {
  return <DashboardView initialTab="creatives" />;
}
